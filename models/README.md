# Models

This directory is reserved for serialized and versioned model artifacts when the CreditWise prototype moves toward deployment.

Generated model binaries are ignored by Git through `.gitignore` until a deliberate model-versioning strategy is established.
