# Source Code

This directory is reserved for reusable training, preprocessing, and prediction modules as the notebook prototype is converted into a maintainable application.

Planned modules:

- `preprocessing.py` - reusable data cleaning and feature transformation
- `train.py` - model training and evaluation
- `predict.py` - inference for new loan applications
